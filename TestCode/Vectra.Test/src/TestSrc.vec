space Vectra.Test;

class Program {
    void Main() {
        PrintLine("Hello Vectra!");
        Print("How old are you? ");
        number age = ReadInt();
        let player = new Player(age);
        PrintLine("Creating observer...");
        let observer = new Observer();
        PrintLine("Observer created.");
        observer.Observe(player);
    }
}