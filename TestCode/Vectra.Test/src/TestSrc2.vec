space Vectra.Test;

class Player {
    number Age { get; set; }
    string Name { get; set; }
    number Wins = 0;
    number Losses = 0;
    Player(number age, string name) {
        this.Age = age;
        this.Name = name;
        PrintLine("Welcome to the game " + this.Name + "!");
        PrintLine("Your age is " + this.Age);
    }
    
    void Win() {
        PrintLine("Player wins");
        this.Wins = this.Wins + 1;
    }
    
    void Lose() {
        this.Losses = this.Losses + 1;
    }
    
    number GetVictoryPercentage() {
        attempt {
            let totalMatches = this.Wins + this.Losses;
            if (totalMatches == 0)
                abort new Error("No matches played");
            let of1 = this.Wins / totalMatches;
            return of1 * 100;
        } debrief {
            PrintLine("Recalculated victory percentage");
        }
    }
}

class Observer {
    void Observe(Player player) {
        PrintLine("Observing player");
    }
}
